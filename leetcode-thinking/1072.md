思路：

遍历每一行，假设当前行在结果当中，意思就是找出所有与当前行中每一列的异或值都相等(都为`1`或者都为`0`)的所有行，

例如，当前行为`[1,0,1,0,1]`

那么`[1,0,1,0,1]`和`[0,1,0,1,0]`都是与它异或值相等的行，异或值分别为`[0,0,0,0,0]`和`[1,1,1,1,1]`，

而`[1,1,1,0,1]`则不是，异或值为`[0,1,0,0,0]`；

对于每一次找到异或值相等的行，使用一个`hash`将行的索引保存起来，后面遇到直接跳过（避免重复计算），因为如果不跳过，那么此行再次找到相同的异或值，一定是已经包含在之前计算的最大结果内。