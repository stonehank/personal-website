思路：

如果不存在`zigzag`那么就很容易算，每次只需要找`Math.floor(n/2)`。

在存在`zigzag`的情况下，由于每一行的顺序跟上一行都不同，因此要找到当前行对应的数字**按照上一行的顺序**的数字。

例如，上一行是`4,5,6,7`, 当前行是`15,14,13...`，那么当前行**第二个**如果按照上一行顺序应该是`9`。

接着对`9`执行`Math.floor(n/2)`。