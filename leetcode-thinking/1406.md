思路：

`dp[i]` 表示在第i个位置，某个人先走，能拿到的最大值

如果 `len<=3`，例如`[1,2,3]`

那么`dp`为`[6,5,3]`

如果`len>3`，例如`[7,1,2,3,7]`

那么`dp`最后3位为`[?, ?,12,10,7]`

当i=1时，`dp[i]`的值，需要计算 当取`k(1-3)`个时，能取到最大值减去`dp[i+k+1]`的值

当i=0时，当`k`只取1个值(7)时，能获得最大值，因为`7 - dp[i+1+1] = 8`

因此最终dp为`[8,-1,12,10,7]`
